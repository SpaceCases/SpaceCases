# .env.example

# Bot token for your discord bot application
BOT_TOKEN=your-bot-token-here

# Whoever owns the discord bot - gives access to certain commands
OWNER_ID=your-discord-id-here

# Optional: domain for SpaceCases assets (defaults to https://assets.spacecases.xyz)
# ASSET_DOMAIN=your-asset-domain

# Database connection settings
DB_USER=your-db-username
DB_PASSWORD=your-db-password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=your-database-name

# Optional: Test Guild ID for syncing slash commands (recommended during development)
# TEST_GUILD=your-test-guild-id-here
